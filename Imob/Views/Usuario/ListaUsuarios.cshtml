@model IEnumerable<Imob.Models.UsuarioLogado>
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Imob</title>
    <link rel="stylesheet" href="~/css/listarLocatarioStyle.css" />
    <script src="https://kit.fontawesome.com/0aa0d458c6.js" crossorigin="anonymous"></script>
</head>
<body>


    <div id="page">
        <aside>
            <header>
                <i class="fab fa-battle-net"></i>
                <h1>Lista de Usuários do sistema</h1>
                <a asp-action="InicialMenu" asp-controller="Home"><i class="fas fa-arrow-left"></i></a>
            </header>
            <footer>
                <strong>Imob</strong>
                <span>Imóveis</span>
            </footer>
        </aside>

        <table class="tabelaClasse">
            <div class="topoDaLista">
                <h4 class="h4add">Adicionar</h4>
                <a asp-action="CadastrarUsuario" asp-controller="Usuario" class="btn-cad"><i class="fas fa-plus"></i></a>
               
            </div>
            <thead>
                <tr>
                    <th class="th_USU">@Html.DisplayNameFor(model => model.Email)</th>
                    <th class="th_USU1">@Html.DisplayNameFor(model => model.Senha)</th>
                    <th class="th_USU2">@Html.DisplayNameFor(model => model.CriadoEm)</th>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>

                @foreach (var item in Model)
                {

                <tr class="tr_classe">
                    <td class="td_USU">@Html.DisplayFor(modelItem => item.Email)</td>
                    <td class="td_USU">@Html.DisplayFor(modelItem => item.Senha)</td>
                    <td class="td_USU">@Html.DisplayFor(modelItem => item.CriadoEm)</td>
                    <td>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn-remover"><i class="far fa-trash-alt"></i></a>
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn-alterar"> <i class="far fa-edit"></i></a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>

</body>
</html>
